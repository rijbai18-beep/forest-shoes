import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test placeholder', (WidgetTester tester) async {
    // Full smoke test requires Firebase initialization.
    // Integration tests are in integration_test/.
    expect(true, isTrue);
  });
}
